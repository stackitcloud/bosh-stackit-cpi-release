platform: linux
image_resource:
  type: registry-image
  source: &mirror_source
    repository: registry.onstackit.cloud/scf-images/terraform
    username: ((harbor.username))
    password: ((harbor.password))

inputs:
- name: repo
outputs:
- name: data

params:
  VAULT:
  VALUES: []

run:
  path: repo/ci/tasks/read-from-secrets-manager/task.sh
